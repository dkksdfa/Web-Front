import styled from "styled-components";

const Title = styled.h1`
  font-size: 3rem;
  font-weight: bolder;
  color: #333;
`;

const Body = styled.div`
  width: 50%;
  margin: 20px auto;
`;

export { Title, Body };
